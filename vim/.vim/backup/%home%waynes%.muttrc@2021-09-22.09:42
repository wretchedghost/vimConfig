# About Me
set from = "waynes@capitanusa.com"
set realname = "Wayne S"

# My Credentials
set smtp_url = "smtp://waynes@capitanusa.com@mail.capitanusa.com:26/"
set smtp_pass = "$Waynes1"
set imap_user = "waynes@capitanusa.com"
set imap_pass = "$Waynes1"

# My Mailboxes
set folder = "imaps://mail.capitanusa.com:143"
set spoolfile = "+INBOX"

# Where To Put Stuff
set header_cache = "~/.mutt/cache/headers"
set message_cachedir = "~/.mutt/cache/bodies"
set certificate_file = "~/.mutt/certificates"

# Etc
set mail_check = 10
set move = no
set imap_keepalive = 900
set sort = threads
set editor = "vim"

# GnuPG boostrap
# source ~/.mutt/gpg.rc
